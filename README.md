# pruebav1 - Proyecto PHP con Login\n\n## Usuario Git\n- Nombre: Soledad BV\n- GitHub: soledad30\n\n## Comandos para Git Commit (ejecuta en terminal desde el directorio del repo):\n\n1. Ver estado: `git status`\n2. Agregar cambios: `git add .` (o `git add archivo.php`)\n3. Commit: `git commit -m \"Tu mensaje descriptivo\"`\n4. Push: `git push` (o `git push origin branch-name`)\n\nEjemplo completo:\n```\ngit checkout -b mi-nueva-feature\ngit add .\ngit commit -m \"Agrego nueva feature\"\ngit push -u origin mi-nueva-feature\n```\n\n## Demo Login PHP\n- Abre `V2.PHP` en servidor PHP (php -S localhost:8000 en pruebav1/)\n- Login: admin / 1234\n- Dashboard protegido.\n\nRepo: https://github.com/soledad30/pruebav1.git
